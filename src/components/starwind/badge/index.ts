import Badge from "./Badge.astro"
import { badge } from "./variants"

const BadgeVariants = {
  badge,
}

const badgeVariants = badge

export { Badge, BadgeVariants, badge, badgeVariants }

export default Badge
